DATABASE_URL=postgres://YourUserName:YourPassword@YourHostname:5432/YourDatabaseName
JWT_SECRET=
COOKIE_SECRET=
PORT=
BCRYPT_SALT=
# Lista de origens permitidas separadas por vírgula (ex.: https://app.exemplo.com,http://localhost:3000).
# Se vazio, requisições cross-origin são bloqueadas por padrão.
CORS_ORIGIN=
POSTGRES_USER=
POSTGRES_PASSWORD=
PGADMIN_DEFAULT_EMAIL=
PGADMIN_DEFAULT_PASSWORD=
# URL do frontend usada para montar o link de redefinição de senha (ex.: https://app.exemplo.com)
FRONTEND_URL=
# Configuração de SMTP para envio de e-mails (redefinição de senha e envio de orçamentos)
# Em desenvolvimento, suba o Mailpit via docker-compose e use os valores abaixo:
# os e-mails enviados ficam visíveis em http://localhost:8025 (nada sai para a internet).
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USER=mailpit
SMTP_PASS=mailpit
SMTP_FROM=orcamentos@local.test
